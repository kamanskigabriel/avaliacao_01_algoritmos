Estrutura base
algoritmo "nome"var  // variáveisinicio  // códigofimalgoritmo

Tipos de variaveis
numero: inteirovalor: realletra: caractereresposta: logico

Condição
se condição entao  // códigosenao  // códigofimse

Repetição
para contador de 1 ate 10 faca  // códigofimpara enquanto condição faca  // códigofimenquanto

Vetor
vet: vetor[1..5] de inteiro para i de 1 ate 5 faca  leia(vet[i])fimpara para i de 1 ate 5 faca  escreva(vet[i])fimpara

Matriz
mat: vetor[1..3, 1..3] de inteiro para linha de 1 ate 3 faca  para coluna de 1 ate 3 faca    leia(mat[linha, coluna])  fimparafimpara

Operadores
>  <  >=  <=  =  <>+  -  *  /  mode  ou  nao

Dicas
mod = resto da divisão (par: mod 2 = 0)
Acumulador: soma <- soma + vet[i]
Maior/menor: inicializa com vet[1] antes do loop
Matriz precisa de dois loops aninhados
escreval pula linha, escreva não pula
